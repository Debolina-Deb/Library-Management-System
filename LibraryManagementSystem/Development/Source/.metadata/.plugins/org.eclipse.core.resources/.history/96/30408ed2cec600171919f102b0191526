/**
 * Validation for Book Details
 */
function validate() {
	var book = document.getElementById("bookForm")

	var validBookId = validateBookId(book)
	var validYOP = validateYOP(book)

	if (validBookId == false || validYOP == false) {
			return false;		
	}
}